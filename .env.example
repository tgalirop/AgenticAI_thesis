# Copy this file to .env only if your launcher loads dotenv files. Never commit
# the real key. PowerShell users may instead set: $env:GROQ_API_KEY = "..."
GROQ_API_KEY=replace_with_your_free_tier_key
